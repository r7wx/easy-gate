services:
  easy-gate:
    build: .
    container_name: easy-gate
    networks:
      - easy-gate-net
    ports:
      - 8080:8080
    volumes:
      - ./easy-gate.json:/etc/easy-gate/easy-gate.json

networks:
  easy-gate-net:
    driver: bridge
